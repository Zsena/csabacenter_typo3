<!DOCTYPE html>
<html lang="en" id="my-scrollbar">

{{> head }}

<body>
  {{> header }}
  <main>
    {{> subpage_banner }}
      <div class="section layout_padding">
          <div class="container">
              <div class="row">
                  <div class="col-md-12">
                      <div class="full">
                          <div class="heading_main text_align_center">
                              <h2><span class="theme_color"></span>Keress minket</h2>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
      </div>
      {{> contact_form_section }}
      {{> contact_cards }}
      {{> map }}
  </main>
  {{> footer }}
</body>

</html>